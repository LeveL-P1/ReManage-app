# Resident Home reference and pop-out implementation plan

> Execution follows the user-locked direction without another design review pause.

1. Create a Home feature catalog with static resident-stack destinations and focused tests for quick actions and feed affordances.
2. Add a reusable full-screen Home feature view and static route wrappers for all Home actions.
3. Replace the existing hero/status Home composition with the reference-inspired action grid, entry-update sheet, post cards, and caught-up ending.
4. Verify focused and full test suites, TypeScript, lint, and the resident web bundle; then provide a visual review link/screenshot before any merge decision.
